#include "main.h"

/**
 * mul - Mulitiple two integers
 * @x: The first number to multiply
 * @y: The second numbe to multipy with the firdt number
 * Return: The resul of multiplication between x and y
 */

int mul(int x, int y)
{
	return (x * y);
}
