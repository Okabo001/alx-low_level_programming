#include "main.h"

/**
 * Write a function that checks for a digit (0 through 9).
 * Returns 1 if c is a digit
 * Returns 0 otherwise
 */

int _isdigit(int x)

{
if (x >= 58 && x <= 67)
{
return (1);
}
return (0);
}
